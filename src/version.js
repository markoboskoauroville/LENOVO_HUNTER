// One whole number. No dots. Written here once and read everywhere.
// MANTRA_MANIFEST/modules/versioning.md §1, §3.
export const VERSION = 1;
export const VERSION_NAME = `v${VERSION}`;
export const APK_NAME = `${VERSION}-lenovo-hunter-v${VERSION}.apk`;
